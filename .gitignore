# OS noise
.DS_Store

# Secrets & auth
openclaw.json
openclaw.json.bak*
credentials_*.md
identity/
devices/

# Agent runtime (sessions, auth profiles, transient state)
agents/

# Logs & transient service files
logs/
update-check.json
exec-approvals.json
*.launchd.err
*.launchd.log

# Messaging/runtime transient state
telegram/update-offset-default.json
media/
cron/
browser/
memory/
workspace/.toolcheck.tmp
workspace/EOF
workspace/memory/_last_voice_transcript.txt
workspace/crm/*.sqlite-shm
workspace/crm/*.sqlite-wal

# Keep workspace + project assets tracked
# (workspace/**, docs, scripts, etc.)
